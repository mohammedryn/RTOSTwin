# Quick Start Guide

The validated runtime path for RTOSTwin now lives in `vnv_final/`.

Use this guide when you want the current, clean, hardware-backed workflow:

- [Validated quick start in `vnv_final/`](../vnv_final/docs/quick_start.md)

## Why this file is short

The older root-level runtime stack was archived during repository cleanup so the
project has a clearer separation between:

- canonical project docs at the repo root
- the validated implementation lane in `vnv_final/`
- legacy root implementation paths preserved under `archive/legacy/`

If you want the currently supported setup path, start in `vnv_final/`.
